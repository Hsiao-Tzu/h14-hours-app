# 紀錄時數（手機版）

好意思文化 5 張「紀錄時數」Google Forms 的手機單頁 App。送出直接寫回原本表單的回應試算表。

- 正式網址：https://hsiao-tzu.github.io/h14-hours-app/
- 加到主畫面：iOS Safari「分享」→「加入主畫面」；Android Chrome 選單「安裝應用程式」。
- 離線可先填，連線後自動送出。

表單欄位若有變動，只改 `index.html` 裡的 `FORMS` / `ENTRY` 兩個常數。
